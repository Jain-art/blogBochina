@using Microsoft.AspNetCore.Identity

@inject SignInManager<blogBochina.Model.Common.BlogPost> SignInManager
@inject UserManager<blogBochina.Model.Common.BlogPost> UserManager

@if (SignInManager.IsSignedIn(User))
{
    <ul class="navbar-nav">
        <li class="nav-item dropdown">
            <a class="nav-link" href="" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <p>
                    Опубликовать пост
                </p>
                <div class="ripple-container"></div>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                <a class="dropdown-item" asp-controller="Blog" asp-action="AddPost">Добавить пост</a>
            </div>
        </li>
    </ul>
}
